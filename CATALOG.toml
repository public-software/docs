# Machine-readable entry for this repository. Aggregated into public-software/catalog.
# Edit here; `pub catalog sync` propagates description, topics and custom properties to GitHub.

[repo]
name        = "docs"
ring        = "spine"          # spine | platform | system | domain | standards
layers      = ["all"]
wave        = 1
tier        = "incubating"        # incubating | stable | core | archived
maintainers = ["maint-docs"]
summary     = "The suite handbook: architecture, contracts, contributor guide, per-repo mdBooks aggregated into one site."

# One [[component]] per crate, app or service this workspace publishes.
# planned: mdBook site · architecture · contributor guide · glossary
#
# [[component]]
# crate     = "pub-docs-example"
# kind      = "lib"                # lib | app | service | plugin | spec
# ledger    = "<ledger entry name>"
# readiness = "none"               # shipped | partial | seed | none
# effort    = 3
# specs     = []
# provides  = ["public:area/iface@1"]
# requires  = []

[[component]]
crate     = "pub-docs-aggregate"   # binary: docs-aggregate
kind      = "app"
ledger    = "mdBook site"
readiness = "seed"
effort    = 2
specs     = []
provides  = []
requires  = []
